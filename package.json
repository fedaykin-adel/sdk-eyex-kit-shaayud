{
  "name": "shaayud",
  "private": true,
  "devDependencies": {
    "typescript": "^5.x",
    "@napi-rs/cli": "^2.x"
  },
  "workspaces": [
    "packages/sdk-*"
  ],
  "scripts": {
    "build:ffi": "cd packages/ffi  && npm run pack",
    "build:node": "cd packages/sdk-node && npm run pack",
    "build:web": "cd packages/sdk-web && npm run pack",
    "run:backend": "cd packages/backend && cargo build && cargo run",
    "build:core":"cd packages/core && cargo clean && cargo build",
    "prepack":"rm -rf package-lock.json && rm -rf node_modules/ && cargo clean && rm -rf target/ &&  rm -rf cargo.lock ",
    "build:pack":"npm run prepack && npm run build:core && npm run build:ffi && npm run build:node && npm run build:web && npm run run:backend",
    "db":"docker compose up -d"
  }
    
}