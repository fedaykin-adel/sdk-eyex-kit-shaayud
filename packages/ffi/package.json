{
  "name": "@shaayud/sdk-core",
  "version": "0.1.1",
  "main": "index.node",
  "files": [
    "index.node",
    "index.d.ts",
    "package.json"
  ],
  "exports": {
    ".": {
      "types": "./index.d.ts",
      "node": "./index.node",
      "default": "./index.node"
    }
  },
  "scripts": {
    "build": "rm -rf index.node && rm -rf node_modules/ && rm -rf package-lock.json && npm install && rm -rf shaayud-sdk-core-0.1.1.tgz && napi build --release",
    "pack": "npm run build && npm pack"
  },
  "devDependencies": {
    "@napi-rs/cli": "^3.0.1"
  }
}
